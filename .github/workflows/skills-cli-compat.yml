name: skills-cli-compat

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:

jobs:
  validate-skills-cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: examples/02-example/package-lock.json
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install validation dependencies
        run: python -m pip install -r requirements-dev.txt
      - name: Install example dependencies
        working-directory: examples/02-example
        run: npm ci
      - name: Validate repository artifacts
        run: npm run validate
      - name: Validate discovery by Agent Skills CLI
        run: bash scripts/tools/check-skills-cli-compat.sh .
