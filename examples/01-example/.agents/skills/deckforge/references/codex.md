# Codex Harness

Use filesystem and shell tools for implementation, and browser/preview tools when available for visual verification. Follow `AGENTS.md` in the target repository. Keep edits scoped, run tests, and surface created files with exact paths. Use the target project's package manager rather than substituting one.
