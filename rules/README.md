# Rules

Priority:

1. `AGENTS.md` / `CLAUDE.md`
2. `rules/*.md`
3. active `skills/*/SKILL.md`
4. loaded built-in workflows and references
5. inline comments

The repository packages Agent Skills, schemas, catalogs, examples, validation, and copyable reference components. It does not own a hosted production service.
