# Installation

```bash
npx skills@latest add tph-kds/deckforge --list
npx skills@latest add tph-kds/deckforge --skill deckforge
```

Project-local installs are preferred when DeckForge should follow one codebase's architecture and design system. Global installs are useful for repeated design exploration, but the agent must still inspect the target project before generating code.
